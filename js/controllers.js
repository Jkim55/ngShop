angular.module('ngShop').controller('homeController',   (function ($scope) {
  // build out homeController
}))

angular.module('ngShop').controller('aboutController', (function ($scope) {
  // build out aboutController
}))

angular.module('ngShop').controller('contactController', function ($scope) {
  // build out contactController
})

angular.module('ngShop').controller('shopController', function ($scope) {
  // build out shopController
})

angular.module('ngShop').controller('cartController', function ($scope) {
  // build out cartController
})

angular.module('ngShop').controller('errorController', function ($scope) {
  // build out errorController
})
